## Summary

<!-- What changed and why (1–3 sentences). -->

## Checklist

- [ ] Tests / lint pass for touched areas (`api/`, `ui/`, etc.).
- [ ] **MK-01 buyer/GTM slice:** `./scripts/verify_mk01_buyer_shipped.sh` passes (or N/A if unrelated).
- [ ] If this PR changes **`catalog/vertical-packs.json`** or adds/renames a **packaged workflow** under `catalog/packaged-workflows/`, paste the completed reviewer table from [catalog/TEMPLATE_QUALITY_RUBRIC.md](catalog/TEMPLATE_QUALITY_RUBRIC.md) (or link the filled copy).

## Risk / rollout

<!-- Migrations, feature flags, compat notes — or N/A. -->
